version: 1.0.{build}
skip_tags: true
os: Windows Server 2012 R2
branches:
  only:
    - master
before_build:
  - git submodule update --init --recursive
  - .\prepare.bat
build_script:
  - .\build.release.bat
