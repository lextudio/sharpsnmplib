{
   "tasks": [
      {
         "name": "dotnet-format",
         "command": "dotnet",
         "args": [ "format", "--include", "${staged}", "--no-restore" ],
         "include": [ "**/*.cs", "**/*.vb" ]
      }
   ]
}
